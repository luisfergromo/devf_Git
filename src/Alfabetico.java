/**
 * 
 */

/**
 * @author egual
 *
 */
import java.util.ArrayList;
import java.util.HashSet;

/*public class Alfabetico {
	public class Logic {

        public static void studList() {
            ArrayList<String> stud = new ArrayList<>();
            ArrayList<Double> uspe = new ArrayList<>();


            ArrayList<String> list = sortA(stud, uspe);
            for (String s : list) {
                View.printer(s);
            }
            View.printer("-");
            for (int i = list.size() - 1; i >= 0; i--) {
                String s = list.get(i);
                View.printer(s);
            }

        }
	    public static void ArrayList<String> sortA(ArrayList<String> st, ArrayList<Double> us) {
	        ArrayList<String> list = new ArrayList<>();
	        String s;
	        for (int i = 0; i < st.size(); i++) {
	            s = st.get(i) + ": " + us.get(i);
	            list.add(s);
	        }
	        HashSet<String> set = new HashSet<>(list);
	        list.clear();
	        list.addAll(set);
	        return list;
	    }
}*/
